<!DOCTYPE html>
<html>
  <head>
    <title>ZOldTimeStrings</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="/styles2.css"> 
  </head>
  <body>
    <% if current_user %>
    <div class="nav">
    <ul>
      <li><%= button_to "Home", families_path, method: :get %></li>
      <li><%= button_to "Makers", makers_path, method: :get %></li>
      <li><%= button_to "My Instruments", user_path(session[:user_id]), method: :get %></li>
      <li><%= button_to "Logout", logout_path, method: :post %></li>
    </ul>
    </div>
      <br>
    <% else %>
      <%= link_to "Login", login_path %><br>
      <%= link_to "Signup", signup_path %><br>
    <% end %>

    <%= yield %><br>
    
  </body>
</html>
